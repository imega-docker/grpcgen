version: 2

defaults: &defaults
  working_directory: ~/project

jobs:
  build:
    <<: *defaults
    machine:
      enabled: true
    steps:
      - checkout
      - run: |
          mkdir build && echo 1234 >> ~/project/build/rootfs.tar.gz
      - persist_to_workspace:
          root: build
          paths:
            - rootfs.tar.gz
  release:
    <<: *defaults
    machine:
      enabled: true
    steps:
      - attach_workspace:
          at: ~/project
      - run: |
          ls -l
workflows:
  version: 2
  test_and_release:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
      - release:
          requires:
            - build
          filters:
            tags:
              only: /[0-9]+(\.[0-9]+)*/
            branches:
              ignore: /.*/
